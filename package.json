{
  "name": "movie-db",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel index.html movies.html --open",
    "serve": "parcel index.html movies.html --open",
    "start": "parcel index.html movies.html --open",
    "build": "parcel build index.html movies.html --no-source-maps --no-cache"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-clean-easy": "^1.0.2",
    "sass": "^1.30.0"
  },
  "parcelCleanPaths": [
    "dist",
    ".cashe"
  ],
  "browserslist": [
    "last 1 Chrome versions"
  ]
}
